import { NextResponse } from 'next/server';

// Phase 2 placeholder
export async function GET() {
  return NextResponse.json({ message: 'Fallback templates — Coming in Phase 2' }, { status: 501 });
}
